---
type RelatedLink = { title: string; href: string };
const { links = [] } = Astro.props as { links?: RelatedLink[] };
---
<section class="card" id="related-guides">
  <h2>Related guides</h2>
  <ul>
    {links.map((link) => <li><a href={link.href}>{link.title}</a></li>)}
  </ul>
</section>
